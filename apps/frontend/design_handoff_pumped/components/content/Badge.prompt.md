**Badge** — a tiny uppercase label for emphasis.

```jsx
<Badge tone="accent">Today</Badge>
<Badge tone="soft">2 PRs</Badge>
```

Keep the text to one or two words. Use `accent` to mark the live/now item, `soft` for quiet counts.
