**ProgressBar** — a soft pill track with an animated accent fill.

```jsx
<ProgressBar value={45} />
```

Use for overall workout completion (sets logged) or goal progress. The fill animates on value change.
