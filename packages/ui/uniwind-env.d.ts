// Uniwind adds `className` to React Native components via a global type
// augmentation. In the app this comes from the generated `src/uniwind.d.ts`;
// the design-system package references uniwind's shipped types directly so it
// can type-check the primitives standalone.
/// <reference types="uniwind/types" />
