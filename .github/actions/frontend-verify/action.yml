name: Frontend verify
description: Install dependencies, then run the repository's shared verify check.

runs:
  using: composite
  steps:
    - name: Setup app
      uses: ./.github/actions/setup-app
      with:
        working-directory: apps/frontend

    - name: Verify formatting, lint, and types
      shell: bash
      run: bun run check:verify
